# Decision Log

- Date: 2026-02-28
- Refactor scope: Full project refactor except raw data preservation.
- Preserved: `01_Data/01_Raw_Data`, boundary raw files, metadata raw files.
- Renamed: `01_Data/02_Shapefiles` -> `01_Data/02_Boundary`.
- Moved metadata: `01_Data/99_Metadata` -> `01_Data/01_Raw_Data/99_Metadata`.
- Archived legacy assets under `_archive_pre_refactor_20260228`.
- Reset processed/output directories for full regeneration.
- 2026-02-28 19:11 update: archive directory deleted after raw-driven pipeline replacement and successful end-to-end rerun.
- Pipeline standard set to `02_Code/99_run_all.R` with 01-17 scripts.
- 2026-03-19 update: typology canonical age-share/speed inputs were harmonized to `60+` (`age60_plus_share`, `aging_speed_*`) for consistency with the quarterly `age60_*` family; `old_age_dependency(to_in_005)` remained the explicit exception at that time. `research_plan.md` stayed frozen and the controlled deviation was recorded in procedure/codebook docs. Main typology feature set moved to `S3_balanced14_age60`. This state was superseded by the 2026-03-20 reference-doc realignment.
- 2026-03-19 update: PCA was removed from typology clustering. The main typology then used the 14 scaled source variables in `S3_balanced14_age60` directly under recipe ID `D0_direct14_age60`. Legacy output filenames `typology_factor_scores.parquet` and `typology_factor_recipe.csv` were retained for compatibility, but now store the direct input matrix and direct-input recipe registry. This state was superseded by the 2026-03-20 reference-doc realignment.
- 2026-03-19 update: typology clustering logic was split into a shared utility module and an optional auxiliary grid-search workflow. The auxiliary script evaluates 33 curated direct-input feature-set combinations in two stages(REDCAP screening then top-10 full ladder), records a manual-promotion leaderboard, and does not modify the canonical main config or `99_run_all.R`.
- 2026-03-19 update: manual promotion from auxiliary typology search was simplified. The main config now resolves `cfg$typology_factor_recipe_id` automatically from `cfg$typology_factor_source_feature_set`, so promoting an auxiliary winner only requires changing the source feature-set ID.
- 2026-03-19 update: typology `k` selection remains gate-based, but the candidate grid was widened to `3:8` and explicit `k` diagnostics were added for both canonical and auxiliary workflows. Main/aux outputs now include `silhouette`, `Calinski-Harabasz`, `Davies-Bouldin`, cluster-balance, and stability views in dedicated diagnostic tables/plots to support data-informed `k` choice without forcing a fully automatic selector.
- 2026-03-20 update: canonical `region_typology` was redefined as a **global type** layer rather than a contiguous spatial regionization. The active main selector now compares `Ward/HCA` first and `KMeans` as fallback on the same direct-input recipe, drops contiguity from the main gate, and uses scale robustness plus `KMeans` seed robustness to decide selection.
- 2026-03-20 update: descriptive spatial regionization was once split into a separate optional workflow. Its outputs live under `region_zone_spatial*` and `zone_*`, remain outside `panel_main`, and are not auto-joined into the main TWFE/SPDM contracts.
- 2026-03-20 update: the earlier `09_aux_explore_typology_specs.R` feature-set search remains in the repository only as a deprecated manual helper. Active codebook/procedure/QC contracts now target the global type main workflow plus the optional spatial zone workflow.
- 2026-03-20 update: the standalone zone runner/validator were folded back into the canonical `09_run_spatial_clustering.R` and `10_validate_typology.R` entrypoints to reduce public workflow surface. Spatial zone outputs remain descriptive-only, but are now always generated together with typology outputs; when the zone gate blocks selection, best-effort reference assignments/profiles/maps are still written.
- 2026-03-20 update: typology/zone quality gates were relaxed slightly after empirical review of `typology_k_comparison.csv` and `zone_k_comparison.csv`. The active thresholds moved from `5% / 65% / 0.02 / 0.60 / 0.80` to `3% / 70% / 0.01 / 0.55 / 0.75` for `min_cluster_share / max_cluster_share / silhouette / mean_ari / min_same_share`; `KMeans` seed robustness thresholds were relaxed in parallel to `seed_mean_ari >= 0.55` and `seed_min_same_share >= 0.75`.
- 2026-03-20 update: canonical typology candidate comparison was refactored into a unified post-hoc benchmark across `Ward/HCA`, `KMeans`, `ClustGeo`, `REDCAP`, `SKATER`, and `max-p`. Method roles are no longer fixed before evaluation; the benchmark writes a shared leaderboard and manual decision sheet, while the production `region_typology` layer follows a separate manual-promotion target in config so benchmark recommendation and current promoted method can diverge explicitly.
- 2026-03-20 update: research-purpose alignment was tightened for the integrated `09/10` typology workflow. The main global benchmark now compares only `Ward/HCA`, `KMeans`, and `ClustGeo`; `REDCAP`, `SKATER`, and `max-p` are zone-only. `KMeans` seed robustness became a hard gate, method-summary/promotion ranking was unified to the same priority sort, and `region_typology.parquet` is no longer written as a blocked best-effort reference. When the explicit promoted target is missing, non-success, or gate-failing, `09_run_spatial_clustering.R` writes diagnostics plus zone outputs and then fails to block downstream typology-dependent steps.
- 2026-03-20 update: `research_plan.md` and supporting reference docs were realigned to the same `60+` elderly definition. The current reference design drops the early/late elderly split and removes `old_age_dependency` from active typology, SGIS side diagnostics, and model specs; any legacy `65+/75+` or `old_age_dependency` fields are now audit-only. The canonical typology reference IDs were updated to `S3_balanced13_age60` and `D0_direct13_age60`.
- 2026-03-20 update: the typology workflow was simplified back toward the original method-agnostic benchmark intent. The active main benchmark again runs `Ward/HCA`, `KMeans`, `ClustGeo`, `REDCAP`, `SKATER`, and `max-p` on the same canonical direct-input recipe, automatically applies the benchmark top candidate to `region_typology`, and retains blocked best-effort reference outputs instead of stopping the pipeline. At that intermediate point, supporting docs were temporarily realigned to `S3_balanced14_age60` and `D0_direct14_age60` with `old_age_dependency_vol` restored as the explicit typology exception. This state was superseded later on 2026-03-20 by the age-definition cleanup that reinstated the `60+`-only 13-feature canonical recipe.
- 2026-03-20 update: the age-definition cleanup was applied to the active pipeline as well as the docs. Current canonical typology and SGIS side diagnostics now use `60+` level/speed only, no longer derive `65+/75+` split variables or `old_age_dependency` as project variables, and use canonical IDs `S3_balanced13_age60` and `D0_direct13_age60`. The six-method `benchmark_auto` typology workflow itself was left unchanged.
- 2026-03-20 update: execution cost of the all-method benchmark was reduced. The active main workflow now runs all six methods only once for the baseline benchmark, computes scale/seed stability only for the baseline shortlist top 3 candidates, and keeps spatial zone generation as a post-typology auxiliary stage within the integrated `09/10` workflow.
- 2026-03-20 update: after dropping `old_age_dependency` from active typology by concept, the canonical feature set was simplified from the transformed 13-feature bundle to a direct, concept-aligned 10-feature recipe. Current canonical IDs are `S3_concept10_age60` and `D0_direct10_age60`, using `age60_plus_share_late`, `aging_speed_long`, four late housing structure variables, `ln_commercial_business_density_late`, `commercial_diversity_index_late`, and the direct composition pair `commercial_food_share_biz_late` + `commercial_retail_share_biz_late`.
- 2026-03-20 update: active typology was simplified again to match the actual research intent of typology rather than regionization. Canonical selection now compares only `Ward/HCA` and `KMeans`; `ClustGeo` remains as comparison-only appendix output; `REDCAP`, `SKATER`, `max-p`, and all `region_zone_spatial*` / `zone_*` products were removed from the active pipeline, validation contract, and codebook.
- 2026-03-20 update: canonical typology source variables were re-tuned against the active `Ward/HCA`/`KMeans` benchmark after empirical review showed that the all-`late`-heavy `S3_concept10_age60` recipe repeatedly produced a tiny outlier cluster and never passed the common gate. The active canonical IDs are now `S3_hybrid10_age60` and `D0_direct10_hybrid_age60`, using `age60_plus_share_late`, `aging_speed_long`, `avg_household_size_trend`, `small_housing_share_late`, `old_housing_share_trend`, `apartment_share_late`, `ln_commercial_business_density_late`, `commercial_diversity_index_late`, and the direct composition pair `commercial_food_share_biz_late` + `commercial_service_share_biz_late`.
- 2026-03-20 update: `09_aux_explore_typology_specs.R` was revived as a supported manual helper instead of a dead deprecated script. The helper is now off-pipeline, compares a small curated registry of 8 direct-input feature sets with `Ward/HCA` and `KMeans` as the selection pool plus `ClustGeo` as comparison-only, keeps legacy `typology_aux_*` output names, and never auto-promotes the canonical main typology.
- 2026-03-21 update: the manual typology feature-set helper was expanded from the initial 8-spec pilot to an 18-spec curated `late + trend` registry. The helper still remains off-pipeline and non-promoting, now comparing `Ward/HCA`, `KMeans`, and `PAM / k-medoids` in the selection pool with `ClustGeo` retained as comparison-only while systematically covering six theory-guided family structures crossed with the three direct composition pairs (`fs`, `fr`, `rs`).
- 2026-03-21 update: the manual typology helper was expanded again from 18 to 30 curated `late + trend` specs after the first broadened search showed that the best-performing candidates clustered around `housing_trend`-heavy families. The second-stage expansion adds 12 nearby theory-guided combinations around the top families rather than opening a full unconstrained grid, keeping the helper off-pipeline while widening the search around `A/H/I` family interactions and `late` versus `trend` composition timing.
- 2026-03-21 update: the helper was expanded once more into a full `72`-spec structured `late + trend` grid covering `A(2) × H(3) × I(2) × pair(3) × composition_stat(2)`. This replaces the staged 30-spec neighborhood search with a fully enumerated theory-guided late/trend grid while still excluding `early`, `recent`, and `vol` from the first-phase search and keeping the helper strictly off-pipeline.
- 2026-03-21 update: after the full 72-spec helper run, the canonical typology source set was promoted from `S3_hybrid10_age60` / `D0_direct10_hybrid_age60` to the helper winner `Saux_alate_htrend_ilate_rs_late_age60` / `Daux_direct10_alate_htrend_ilate_rs_late_age60`. That intermediate canonical recipe used `age60_plus_share_late`, `aging_speed_long`, four housing trend variables, `ln_commercial_business_density_late`, `commercial_diversity_index_late`, and the direct composition pair `commercial_retail_share_biz_late` + `commercial_service_share_biz_late`.
- 2026-03-21 update: SeMAS-linked typology industry variables were retired after methodological review. The active pipeline now derives typology industry structure only from SGIS business/employee aggregates. Canonical source-set selection is driven by a 16-spec off-pipeline helper that fixes the 6 aging/housing inputs and searches `late/trend` combinations over `ln_total_business_density`, `industry_entropy_biz`, `ln_employee_density`, and `employee_per_business`.
- 2026-03-21 update: after the SGIS-only industry helper run, the canonical typology source set was promoted to `Saux_sgis_bdtrend_betrend_edtrend_epblate_age60` / `Daux_direct10_sgis_bdtrend_betrend_edtrend_epblate_age60`. The active canonical recipe now uses `age60_plus_share_late`, `aging_speed_long`, four housing trend variables, `ln_total_business_density_trend`, `industry_entropy_biz_trend`, `ln_employee_density_trend`, and `employee_per_business_late`.
- 2026-03-21 update: after direct ablation comparison, `ln_employee_density_trend` was removed from the active canonical typology source set because it was highly collinear with `ln_total_business_density_trend` while providing only limited incremental benchmark gain. `ln_employee_density` remains in `typology_features` as an expansion/appendix variable, but the active helper shrank from a 16-spec four-industry-variable grid to an 8-spec three-industry-variable grid over `ln_total_business_density`, `industry_entropy_biz`, and `employee_per_business`.
- 2026-03-21 update: the active SGIS-only canonical typology source set is now `Saux_sgis_bdtrend_betrend_epblate_age60` / `Daux_direct9_sgis_bdtrend_betrend_epblate_age60`, using `age60_plus_share_late`, `aging_speed_long`, four housing trend variables, `ln_total_business_density_trend`, `industry_entropy_biz_trend`, and `employee_per_business_late`.
- 2026-03-21 update: after rerunning the reduced 8-spec SGIS-only helper, the best-performing no-employee-density recipe was `Saux_sgis_bdtrend_belate_epblate_age60` / `Daux_direct9_sgis_bdtrend_belate_epblate_age60`. The active canonical typology was promoted to the neutral main IDs `Scanon_sgis_bdtrend_belate_epblate_age60` / `Dcanon_direct9_sgis_bdtrend_belate_epblate_age60`, keeping `ln_total_business_density_trend` but switching `industry_entropy_biz` from `trend` to `late` while retaining `employee_per_business_late`.
- 2026-03-22 update: after the canonical-vs-ablation comparison, the main typology recipe was promoted to the drop-2 variant `Scanon_sgis_drop_hhsize__smallhs_age60` / `Dcanon_direct7_sgis_drop_hhsize__smallhs_age60`. The active canonical source set now removes `avg_household_size_trend` and `small_housing_share_trend`, while retaining `age60_plus_share_late`, `aging_speed_long`, `old_housing_share_trend`, `apartment_share_trend`, `ln_total_business_density_trend`, `industry_entropy_biz_late`, and `employee_per_business_late`. The ablation helper remains in the repository as a historical manual tool, but current config disables it.
- 2026-03-20 update: `PAM / k-medoids` was added to the active typology benchmark as a third aspatial main-selection method. The canonical main and manual helper now compare `Ward/HCA`, `KMeans`, and `PAM` in the selection pool while keeping `ClustGeo` as comparison-only. `PAM` runs on the same scaled direct-input matrix with Euclidean distance and does not add a separate seed-robustness gate.
- 2026-03-21 update: `typology_features` was extended with `population_density` and `ln_population_density` annual source variables plus their typology feature derivatives. Canonical typology remains `S3_hybrid10_age60`, but an off-pipeline experimental clone now runs `S3_hybrid11_popdensity_age60` / `D0_direct11_hybrid_popdensity_age60` and writes `_popdensity`-suffixed outputs so population-density sensitivity can be compared without mutating the main workflow.
- 2026-03-21 update: the off-pipeline population-density clone was retired after the density sensitivity runs and curated `late/trend/recent/vol` search failed to beat the canonical `S3_hybrid10_age60` typology. `_popdensity` scripts, outputs, and density-specific typology expansion contracts were removed to restore a single canonical workflow.
- 2026-03-27 update: the active main typology recipe was promoted to the 4-variable core set `Scanon_sgis_core4_age60` / `Dcanon_direct4_sgis_core4_age60`, using `age60_plus_share_late`, `aging_speed_long`, `ln_total_business_density_trend`, and `employee_per_business_late`. The previous 7-variable recipe `Scanon_sgis_drop_hhsize__smallhs_age60` is retained as a legacy fixed-helper baseline for housing/industry ablation scripts, while the disabled historical `09_aux_explore_typology_ablation.R` now explicitly references the pre-drop-2 9-variable baseline `Slegacy_sgis_pre_drop2_age60`.
- 2026-03-29 update: deprecated manual typology helpers (`09_aux_explore_typology_*`) and manual TWFE helper experiments (`11b`~`11h`) were removed from the active repository surface together with their stale exploratory outputs. The canonical typology/main-impact pipeline, robustness scripts, and optional sidecars remain unchanged.
- 2026-04-18 update: the project design was pivoted from the typology-first workflow to a spatial-core workflow. `research_plan.md` was redefined as the active editable design anchor and fully replaced in place; the prior contents were archived as `research_plan_legacy_20260418.md`.
- 2026-04-18 update: the active methodology stack is now `ESDA -> TWFE baseline / residual spatial-diagnostic -> SPDM main global model -> GTWR resident-only optional local sidecar`. Typology and `region_type` were retired from the canonical required workflow and reclassified as legacy/manual assets.
- 2026-04-18 closeout update: the implementation surface was aligned to the spatial-core workflow. `02_Code/00_config/config.R` now treats the active runtime as the default surface, adds `RUN_GTWR_MAIN_SIDECAR` with default `FALSE`, and disables retired GTWR/GWR sidecars by default. `02_Code/99_run_all.R` now runs only the canonical active sequence and conditionally appends `14_run_gtwr_optional.R` when the GTWR sidecar is explicitly enabled.
- 2026-04-18 closeout update: active QC/reporting/codebook contracts were narrowed to the canonical workflow. `02_Code/06_qc/17_validate_method_dataset_alignment.R` was rewritten to validate only shared data, ESDA, TWFE, SPDM, W robustness, robustness, reporting, and opt-in GTWR raw outputs. `02_Code/04_models/16_make_tables_figures.R`, `04_Docs/02_Codebook/04_model_spec.md`, and `04_Docs/02_Codebook/99_spec_to_code_map.csv` were aligned so typology and appendix families no longer define active success.
- 2026-04-18 closeout update: the TWFE reporting contract bug was fixed in `02_Code/04_models/11_run_twfe_main.R` so the supplementary coefficient plot now maps to the actual `supplementary_vitality` outcome group.
- 2026-04-18 closeout update: existing active outputs satisfy the current contract snapshot. The active output registry is present at `41/41`, `03_Output/04_Logs/method_dataset_contract_check.csv` reports `23 PASS / 0 FAIL`, and current-mode GTWR raw outputs exist, but these checks rely on existing outputs rather than a fresh rerun performed in the closeout step.
- 2026-04-18 closeout update: at that intermediate point, remaining closeout work was limited to one fresh default rerun, one opt-in GTWR rerun, and then deletion of `TEMP_SPATIAL_CORE_REFACTOR.md` after the final completion evidence was no longer needed. This state was superseded later on 2026-04-18 by the fresh rerun and GTWR deferral updates below.
- 2026-04-18 closeout update: a fresh canonical rerun was completed through `02_Code/99_run_all.R`. The pipeline ended at `2026-04-18 12:02:45`, and the fresh active QC snapshot written at `2026-04-18 12:02:44` reports `method_dataset_contract_check.csv = 22 PASS / 0 FAIL`.
- 2026-04-18 closeout update: an opt-in `GTWR_MODE=standard` rerun was started for fresh sidecar evidence, but it was then explicitly deferred by user direction because standard mode is a multi-day job. No fresh standard-mode GTWR output files were written, temporary lock/log artifacts from the interrupted attempt were cleaned up, and GTWR standard validation is no longer treated as part of the routine closeout path.
- 2026-04-18 closeout update: `TEMP_SPATIAL_CORE_REFACTOR.md` was removed after its operational conclusions were transferred into this log. The routine spatial-core closeout is now represented by the active design documents plus this decision log, without a temporary control note.
- 2026-04-18 update: the project surface was further simplified from “spatial-core with legacy SGIS/typology remnants” to a full SGIS/typology purge. Active docs, templates, codebook specs, config, QC helpers, and appendix scripts now treat the project as a Seoul short-run workflow only, with no SGIS, concordance, typology, or `region_type` contract remaining outside this log.
- 2026-04-18 update: tracked SGIS/typology legacy assets were deleted from the repository surface, including `research_plan_legacy_20260418.md`, concordance/SGIS preprocess scripts, typology runners, the strict exact-year SGIS QC helper, and `utils_typology_clustering.R`. Mixed appendix scripts were retained only in reduced form at that time, while `11b`/`13b` covered COVID-only interaction appendices.
- 2026-04-18 update: generated SGIS/typology/concordance processed and output artifacts were designated for purge from `01_Data/03_Processed_Data/**` and `03_Output/**`. Protected raw SGIS source folders under `01_Data/01_Raw_Data/**` were not modified and remain manual external cleanup targets if permanent data retirement is desired beyond the repository working surface.
- 2026-04-19 update: the active short-run Seoul workflow was extended from `2019Q1-2025Q3` to `2019Q1-2025Q4` after the Seoul Commercial Service raw branch was updated with `2025Q4` observations. The active design docs, codebook, config, preprocessing horizon, and QC rules were aligned to the new terminal quarter.
- 2026-04-19 update: the Seoul raw integration contract explicitly preserves the mixed raw layout. `sales` and `store` remain split-year sources, while `floating`, `resident`, `worker`, `income`, `facility`, `apartment`, and `change` remain single-file multi-year sources. `03_build_seoul_quarter_panel.R` now validates source coverage by source type and parsed latest `yq` rather than by uniform file-count assumptions.
- 2026-04-19 update: after the `2025Q4` horizon extension, a fresh canonical rerun through `02_Code/99_run_all.R` completed successfully at `2026-04-19 17:34:52`. The refreshed active QC snapshot reports `03_Output/04_Logs/method_dataset_contract_check.csv = 22 PASS / 0 FAIL`, and the generalized processed-parquet QC reports `03_Output/04_Logs/processed_parquet_qc_checks.csv = 33 PASS / 0 FAIL`.
- 2026-04-19 update: `panel_main.parquet` now extends to `2025Q4`, and the refreshed SPDM main outputs report `sample_max_yq = 2025Q4`. Optional GTWR raw outputs were not rerun in this pass and remain at `2025Q3`. To avoid stale downstream reporting, `16_make_tables_figures.R` now suppresses `gtwr_delta_*` tables unless the raw GTWR bundle is horizon-aligned with the current canonical endpoint.
- 2026-04-22 update: the active canonical analysis unit was pivoted from quarterly `adm_cd x yq` to annual `adm_cd x year`. Quarterly code and data were explicitly backed up outside the active workflow and are no longer part of the current design contract.
- 2026-04-22 update: the refactor rationale was fixed at the document level. Only Seoul Commercial Service `sales`, `store`, and `floating` sources remain genuinely quarterly, while many core structural and auxiliary variables are annual or static. Quarterly expansion of those annual variables created repeated within-year values and materially increased local multicollinearity risk for GTWR.
- 2026-04-22 update: phase 1 of the annual refactor rewrote the active design docs, codebook specs, and templates to the annual contract. Canonical time range is now `2019~2025`, canonical lag is `_l1`, TWFE time fixed effects are `year`, the COVID appendix flag is `2020~2022`, and quarterly file names that still remain in implementation code are temporary path aliases to be removed in the code phase.
- 2026-04-22 update: the annualization rule was fixed as follows. Additive flows use annual sums, level/share/density variables use annual representative means with denominator weighting when available, temporal and stability components are recomputed from within-year source distributions, and annual/static auxiliary variables join directly on `adm_cd-year` without quarter-step expansion.
- 2026-04-22 update: an active-document audit removed the remaining quarterly implementation aliases from design and codebook references. Active docs now refer only to the annual preprocessing entrypoint `03_build_seoul_year_panel.R`; quarterly wording remains only where it describes raw-source periodicity or historical decisions in this log.
- 2026-04-23 update: the active annual contract was tightened from `lag-1 annual` to `strict contemporaneous annual`. The shared panel now treats `adm_cd x year` same-year values as canonical inputs for main and appendix models, and `_l1` / `_f1` timing variables were removed from the shared publication contract.
- 2026-04-23 update: `panel_main_pre_vitality.parquet` and `panel_main.parquet` were redefined as no-shift annual panels. Active and appendix TWFE/SPDM/GTWR families now read contemporaneous exposure/control names only, and robustness no longer generates a placebo lead specification.
- 2026-04-23 update: the zero-shift pivot restores the full `2019~2025` annual horizon to the default estimators and treats timing sensitivity as an optional future appendix question rather than a canonical requirement. Active docs, codebook specs, templates, QC, and reporting were aligned to the new contemporaneous annual contract.
- 2026-04-26 update: the resident-only GTWR sidecar was realigned to the GWmodel contract. `14_run_gtwr_optional.R` now uses opt-in execution by default, official observation-point GTWR calibration without explicit `regression.points`, outcome-overlap control exclusion, and latest-year coverage QC.
- 2026-04-26 follow-up: GTWR local collinearity diagnostics were upgraded from a cross-sectional neighbor proxy to a GTWR spatiotemporal local condition-number diagnostic. `14_run_gtwr_optional.R` now constructs one `GWmodel::st.dist()` matrix per spec and reuses it for `bw.gtwr()`, `gtwr()`, and local_CN calculation using the same kernel, adaptive setting, `lamda`, and `ksi`.
- 2026-04-27 update: `14_run_gtwr_optional.R` gained an opt-in `lamda` sensitivity surface. When `RUN_GTWR_LAMDA_SENSITIVITY=TRUE`, it estimates the `GTWR_LAMDA_SENSITIVITY_GRID` values by spec, caches them separately, and writes `gtwr_lamda_sensitivity_<control_set>_<mode>.csv` with latest-year beta agreement, absolute delta, sign flip, and local condition-number diagnostics relative to the main `lamda`.
- 2026-04-28 update: main GTWR fixed adaptive bandwidth was raised from `GTWR_ST_BW=90` to `GTWR_ST_BW=120`. The change follows local sidecar diagnostics showing that `90` could estimate the lean control set but caused singular local design matrices in the extended control set, while `120` retained high agreement with the lean baseline and allowed extended sensitivity runs to estimate.
- 2026-04-28 update: `14_run_gtwr_optional.R` gained an opt-in fixed bandwidth sensitivity surface. When `RUN_GTWR_BANDWIDTH_SENSITIVITY=TRUE`, it estimates the `GTWR_BANDWIDTH_SENSITIVITY_GRID` values `60,90,120,150,180` by spec, caches them separately, and writes `gtwr_bandwidth_sensitivity_<control_set>_<mode>.csv` with latest-year beta agreement, absolute delta, sign flip, and local condition-number diagnostics relative to the main `GTWR_ST_BW=90`.
- 2026-04-27 update: the separate ESDA sidecar was retired from the repository surface. Main ESDA is now represented only by `08_run_esda.R`; the former sidecar script, associated codebook rows, prefixed output contracts, and related panel-view config were removed.
- 2026-04-27 update: main GTWR bandwidth was fixed at adaptive `GTWR_ST_BW=90` by default. Automatic `bw.gtwr()` search is no longer the active default and is retained only as an explicitly requested sensitivity/diagnostic strategy.
- 2026-04-27 update: main GTWR output and cache paths now include `control_set` before `mode`, e.g. `gtwr_main_models_lean_slow.csv` and `gtwr_spec_cache/slow/lean/main/`, so lean and extended GTWR runs can coexist without overwriting each other.
- 2026-04-27 update: ESDA EHSA was aligned to the `sfdep::emerging_hotspot_analysis()` Gi* convention by using queen contiguity with self-neighbor included (`queen_include_self`) and by normalizing the sfdep `oscilating` class spelling to `oscillating` in exported tables/maps.
- 2026-04-27 update: ESDA LISA quadrant labeling was tightened to the standard sign convention. Univariate LISA now classifies clusters with `z(x)` and `W z(x)`, while bivariate LISA uses `z(x)` and `W z(y)`.
- 2026-04-27 update: ESDA Global Moran's I now uses deterministic permutation p-values, and bivariate LISA maps are generated for every computed aging-by-vitality pair rather than only representative composite pairs.
- 2026-05-01 update: Seoul Living Population external inflow was promoted into the default annual preprocessing contract. `04a_build_living_population_inflow.R` builds `living_population_external_inflow.parquet` from INNER/METRO monthly ZIP files as annual mean point-in-time population, `05_build_analysis_panel.R` joins it on `adm_cd-year`, and `vitality_sub_social` now combines `ln_floating_pop` with `ln_external_inflow_pop`.
- 2026-05-02 update: `04a_build_living_population_inflow.R` now computes Seoul Living Population external inflow as monthly normalized annual mean point population. It first averages valid date-hour rows within each `adm_cd-month`, then averages monthly means to `adm_cd-year`; partial-month source coverage is retained in `living_population_inflow_manifest.csv` through `month_success_days`, `month_expected_days`, and `month_coverage_flag`.
- 2026-05-02 update: `bus_stop_count_aux` and `subway_station_count_aux` were retained as source/support variables but replaced in TWFE/SPDM/GTWR control contracts by `transit_accessibility`, a pooled z-score mean of the two annual auxiliary counts.
- 2026-05-01 update: worker-population variables were deactivated from the active analytical contract because Seoul Commercial Service worker-population coverage is missing for 11 administrative dongs across the full 2019~2025 annual horizon. Raw worker source integration remains as provenance in the Seoul annual base builder, but active TWFE/SPDM/GTWR controls, sector-share exposure families, codebook specs, templates, QC schema checks, and final panel model views now exclude `ln_worker_pop`, worker density transforms, and `age60_worker_share`.
- 2026-05-02 update: `ln_floating_pop` was removed from the active TWFE/SPDM/GTWR main control contracts because it is a component of `vitality_sub_social` and the composite vitality indices. The variable remains in `panel_main` as outcome support and vitality input, while control-specific outcome-overlap exclusion was retired from the active main-model contract.
- 2026-05-03 update: `vitality_sub_economic` was simplified from the three-component bundle `ln_sales_count + ln_total_store_count + ln_sales_per_store` to the two-component bundle `ln_sales_count + ln_sales_per_store`. `ln_total_store_count` remains in `panel_main` as denominator/descriptive support but no longer directly enters the economic vitality subindex.
- 2026-05-05 update: `vitality_sub_economic` was revised from `ln_sales_count + ln_sales_per_store` to `ln_sales_count + ln_total_sales`. `ln_sales_per_store` remains in `panel_main` as descriptive/diagnostic support but no longer directly enters the active economic vitality subindex.
- 2026-05-05 follow-up: `vitality_sub_economic` was revised to a two-axis design. The transaction scale axis is the mean of `pooled_z(ln_sales_count)` and `pooled_z(ln_total_sales)`, and the store supply axis is `pooled_z(ln_total_store_count)`; the final economic subindex gives equal weight to the two axes. `ln_sales_per_store` remains descriptive/diagnostic support only.
- 2026-05-17 update: `vitality_sub_economic` was revised to use only the transaction scale definition, `mean(pooled_z(ln_sales_count), pooled_z(ln_total_sales))`. `ln_total_store_count` remains in `panel_main` for store-related diagnostics and derived support variables but is excluded from the active economic subindex and reporting economic components.
- 2026-05-17 update: active TWFE/SPDM controls were reduced to `ln_resident_pop`, `ln_official_land_price`, and `transit_accessibility`; GTWR `extended` now adds only `transit_accessibility` to the lean resident-population and land-price controls. `ln_apartment_household_count`, `hospital_count_aux_core`, and `mall_count_aux_core` remain in `panel_main` as diagnostics/support variables but are excluded from active statistical control contracts.
- 2026-05-03 update: `vitality_sub_stability` was reweighted from a flat three-component mean to a two-axis structural-stability design. The diversity axis is `pooled_z(diversity_index)`, the continuity axis is the mean of `pooled_z(operating_months_rel_seoul)` and `pooled_z(stability_score)`, and the final stability subindex gives equal weight to the diversity and continuity axes.
- 2026-05-03 update: the active apartment control was changed from Seoul Commercial Service `apartment_count` to Seoul apartment-registry household stock. `04_build_auxiliary_covariates.R` now point-matches Seoul apartment registry records to administrative dongs, falls back to unique dong-name/geocode matching for missing coordinates, builds approval-year active stock by `adm_cd-year`, and publishes `apartment_household_count` plus `ln_apartment_household_count`; TWFE, SPDM, and GTWR extended contracts now use `ln_apartment_household_count`.
- 2026-05-03 follow-up: two known Seoul apartment-registry household anomalies are corrected in code rather than by modifying raw data. `A13071302` / 래미안크레시티 is overridden from raw `0` households to `2,397`, and `A10021005` / 래미안신반포팰리스 is overridden from raw missing households to `843`; `04_build_auxiliary_covariates.R` preserves the cleaned raw value and manual-fix note in the apartment preagg audit table.
- 2026-05-03 follow-up: remaining coordinate-missing Seoul apartment-registry unmatched records are corrected in code through `apartment_code -> adm_cd` manual fixes after address verification against the 2020 administrative-dong boundary. The seven fixes are `A10023604` 고척아이파크MD -> 고척1동, `A10023955` 에피소드수유838 -> 수유3동, `A10023372` BX201 청년주택 -> 낙성대동, `B20161229` 신월신도브래뉴2차 -> 신월5동, `B11680001` 일원대우 -> 일원2동, `B11680041` 도곡3차아이파크 -> 도곡1동, and `B11680060` 도곡2차 I-Park -> 도곡1동. `A10023955` household count is also overridden from raw `558` to the verified `818`; raw files remain unchanged and the preagg audit table records manual adm and household fix columns.
- 2026-05-04 update: Seoul Commercial District Service new-enterprise survival rates were added as an annual preprocessing layer. `04b_build_golmok_survival_rate.R` calls `selectSurvivalRate.json` for base years `2019`, `2022`, and `2025` Q4, saves raw JSON responses, publishes `golmok_survival_rate.parquet`, and joins `survival_3y` into `panel_main_pre_vitality`. `vitality_sub_stability` now uses a two-axis design where the continuity axis is `pooled_z(operating_months_rel_seoul)` plus `pooled_z(survival_3y)`; `closure_rate` and `stability_score` remain diagnostic support only. Zero-cohort survival rows remain `NA` and are tracked in `golmok_survival_rate_qc.csv`.
- 2026-05-05 update: GTWR reporting was aligned to the final control-set naming contract. `02_build_gtwr_level_artifacts.R` now accepts only strict `lean` and `extended` GTWR source suffixes, writes report artifacts with the same suffixes, and no longer preserves legacy mode tags in artifact filenames or manifests.
- 2026-05-14 update: the code surface was renamed from incremental historical numbering to a role-based folder structure with folder-local numbering. Canonical scripts now live under `01_preprocess`, `02_esda`, `03_models`, `04_robustness`, `05_reporting`, and `06_qc`; opt-in preprocessing/model sidecars live under `80_optional`; setup moved from `00_config` to `00_setup`; and `99_run_all.R` became `run_all.R`. The migration map is preserved in `04_Docs/03_Log/code_file_rename_migration_20260514.csv`. Output file names were intentionally kept stable.
